<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Portal de Vagas e Ficha de Candidatura - Grupo Brasa</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=Cabinet+Grotesk:wght@700;800;900&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Plus Jakarta Sans', sans-serif; }
    h1, h2, h3, .font-display { font-family: 'Cabinet Grotesk', sans-serif; }
    @media print {
      .no-print { display: none !important; }
      body { background: white !important; font-size: 11pt !important; }
      .print-only { display: block !important; }
    }
  </style>
<script src="/_aistudio-iframe.js?v=2c34bde1"></script></head>
<body class="bg-slate-50 text-slate-900 min-h-screen">
  
  <!-- Header -->
  <header class="sticky top-0 z-40 bg-white/95 backdrop-blur-md border-b border-slate-200 no-print">
    <div class="max-w-7xl mx-auto px-4 h-16 flex items-center justify-between">
      <div class="flex items-center gap-2">
        <div class="w-9 h-9 rounded-lg bg-amber-600 text-white flex items-center justify-center font-bold">GB</div>
        <div>
          <span class="text-lg font-bold text-slate-900 block font-display leading-tight">Grupo Brasa</span>
          <span class="text-[11px] text-slate-500 uppercase tracking-wide">Portal de Vagas & Talentos</span>
        </div>
      </div>
      <div class="flex items-center gap-2">
        <button onclick="baixarEsteHtml()" class="px-3 py-1.5 text-xs font-semibold text-amber-900 bg-amber-50 hover:bg-amber-100 border border-amber-200 rounded-lg flex items-center gap-1">
          📥 Baixar HTML
        </button>
        <button id="btnVoltarForm" onclick="mostrarView('form')" class="hidden px-3 py-1.5 text-xs font-semibold text-slate-700 bg-slate-100 hover:bg-slate-200 rounded-lg">Ver Ficha Pública</button>
        <button id="btnAbrirAdmin" onclick="abrirModalSenha()" class="px-4 py-2 text-xs font-semibold text-white bg-slate-900 hover:bg-slate-800 rounded-lg flex items-center gap-1.5">
          🔒 Área do Recrutador
        </button>
      </div>
    </div>
  </header>

  <!-- VIEW: FORMULÁRIO DO CANDIDATO -->
  <main id="viewForm" class="max-w-4xl mx-auto px-4 py-8">
    <div class="bg-amber-50 border border-amber-200 rounded-2xl p-4 mb-6 flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4">
      <div>
        <div class="flex items-center gap-2">
          <span class="text-[11px] font-bold text-amber-800 uppercase tracking-wider">Vaga Selecionada para esta Ficha</span>
          <span class="text-[10px] bg-amber-200 text-amber-900 px-2 py-0.5 rounded font-semibold">🔒 Definida pelo Recrutador</span>
        </div>
        <h2 id="displayCargo" class="text-xl font-bold text-slate-900 font-display mt-0.5">Churrasqueiro Profissional</h2>
        <p class="text-xs text-slate-600">Somente a administração pode definir ou alterar o cargo desta ficha.</p>
      </div>
      <button onclick="abrirModalSenha()" class="text-xs font-medium text-amber-900 bg-amber-100 hover:bg-amber-200 px-3 py-1.5 rounded-lg whitespace-nowrap">Alterar Cargo (Admin)</button>
    </div>

    <form id="candidateForm" onsubmit="enviarCandidatura(event)" class="space-y-6 bg-white border border-slate-200 rounded-2xl p-6 sm:p-8 shadow-sm">
      <h3 class="text-base font-bold text-slate-900 pb-2 border-b border-slate-200 font-display">1. Dados Pessoais</h3>
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <div class="sm:col-span-2">
          <label class="block text-xs font-semibold text-slate-700 mb-1">Nome Completo *</label>
          <input type="text" id="campoNome" required placeholder="Ex: Carlos Eduardo de Oliveira" class="w-full px-3.5 py-2 text-sm border rounded-xl">
        </div>
        <div>
          <label class="block text-xs font-semibold text-slate-700 mb-1">Data de Nascimento</label>
          <input type="date" id="campoNasc" onchange="calcularIdade(this.value)" class="w-full px-3.5 py-2 text-sm border rounded-xl">
        </div>
        <div>
          <label class="block text-xs font-semibold text-slate-700 mb-1">Idade *</label>
          <input type="number" id="campoIdade" required min="14" max="99" placeholder="Ex: 28" class="w-full px-3.5 py-2 text-sm border rounded-xl">
        </div>
        <div>
          <label class="block text-xs font-semibold text-slate-700 mb-1">CPF</label>
          <input type="text" id="campoCpf" placeholder="000.000.000-00" class="w-full px-3.5 py-2 text-sm border rounded-xl">
        </div>
        <div>
          <label class="block text-xs font-semibold text-slate-700 mb-1">RG</label>
          <input type="text" id="campoRg" placeholder="RG / Órgão Emissor" class="w-full px-3.5 py-2 text-sm border rounded-xl">
        </div>
        <div>
          <label class="block text-xs font-semibold text-slate-700 mb-1">Telefone / WhatsApp *</label>
          <input type="text" id="campoTel" required placeholder="(11) 99999-9999" class="w-full px-3.5 py-2 text-sm border rounded-xl">
        </div>
        <div>
          <label class="block text-xs font-semibold text-slate-700 mb-1">E-mail *</label>
          <input type="email" id="campoEmail" required placeholder="email@exemplo.com" class="w-full px-3.5 py-2 text-sm border rounded-xl">
        </div>
      </div>

      <h3 class="text-base font-bold text-slate-900 pb-2 border-b border-slate-200 font-display pt-4">2. Endereço</h3>
      <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
        <div>
          <label class="block text-xs font-semibold text-slate-700 mb-1">CEP</label>
          <input type="text" id="campoCep" onblur="buscarCep(this.value)" placeholder="00000-000" class="w-full px-3.5 py-2 text-sm border rounded-xl">
        </div>
        <div class="sm:col-span-2">
          <label class="block text-xs font-semibold text-slate-700 mb-1">Logradouro / Rua e Número</label>
          <input type="text" id="campoRua" placeholder="Rua..." class="w-full px-3.5 py-2 text-sm border rounded-xl">
        </div>
        <div>
          <label class="block text-xs font-semibold text-slate-700 mb-1">Bairro</label>
          <input type="text" id="campoBairro" placeholder="Bairro" class="w-full px-3.5 py-2 text-sm border rounded-xl">
        </div>
        <div>
          <label class="block text-xs font-semibold text-slate-700 mb-1">Cidade *</label>
          <input type="text" id="campoCidade" required placeholder="Cidade" value="São Paulo" class="w-full px-3.5 py-2 text-sm border rounded-xl">
        </div>
        <div>
          <label class="block text-xs font-semibold text-slate-700 mb-1">Estado (UF)</label>
          <input type="text" id="campoUf" placeholder="SP" value="SP" class="w-full px-3.5 py-2 text-sm border rounded-xl">
        </div>
      </div>

      <h3 class="text-base font-bold text-slate-900 pb-2 border-b border-slate-200 font-display pt-4">3. Escolaridade</h3>
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <div>
          <label class="block text-xs font-semibold text-slate-700 mb-1">Nível de Escolaridade</label>
          <select id="campoEscolaridade" class="w-full px-3.5 py-2 text-sm border rounded-xl bg-white">
            <option value="Ensino Médio Completo">Ensino Médio Completo</option>
            <option value="Ensino Fundamental Completo">Ensino Fundamental Completo</option>
            <option value="Ensino Técnico">Ensino Técnico</option>
            <option value="Superior Cursando">Superior Cursando</option>
            <option value="Superior Completo">Superior Completo</option>
          </select>
        </div>
        <div>
          <label class="block text-xs font-semibold text-slate-700 mb-1">Instituição / Curso</label>
          <input type="text" id="campoInst" placeholder="Nome da escola ou faculdade" class="w-full px-3.5 py-2 text-sm border rounded-xl">
        </div>
      </div>

      <h3 class="text-base font-bold text-slate-900 pb-2 border-b border-slate-200 font-display pt-4">4. Experiência & Cursos</h3>
      <div>
        <label class="block text-xs font-semibold text-slate-700 mb-1">Experiência Profissional (Empresas, Cargos e Períodos)</label>
        <textarea id="campoExp" rows="3" placeholder="Ex: Restaurante Bom Sabor - Churrasqueiro (2022 a 2024) - Preparo de carnes e cortes..." class="w-full px-3.5 py-2 text-sm border rounded-xl"></textarea>
      </div>
      <div>
        <label class="block text-xs font-semibold text-slate-700 mb-1">Cursos e Certificações</label>
        <textarea id="campoCursos" rows="2" placeholder="Ex: Manipulação de Alimentos (SENAC - 40h)..." class="w-full px-3.5 py-2 text-sm border rounded-xl"></textarea>
      </div>

      <button type="submit" class="w-full py-3.5 bg-amber-600 hover:bg-amber-700 text-white font-bold rounded-xl shadow transition-all">
        Enviar Minha Ficha de Candidatura
      </button>
    </form>
  </main>

  <!-- VIEW: PAINEL DO RECRUTADOR -->
  <main id="viewAdmin" class="hidden max-w-7xl mx-auto px-4 py-8">
    <div class="flex justify-between items-center mb-6">
      <div>
        <h2 class="text-2xl font-bold font-display">Painel do Recrutador (Acesso Autorizado)</h2>
        <p class="text-xs text-slate-500">Gestão de currículos com data de cadastro e exportação em PDF</p>
      </div>
      <div class="flex gap-2">
        <button onclick="definirNovoCargo()" class="px-3.5 py-2 bg-amber-600 hover:bg-amber-700 text-white rounded-lg text-xs font-bold">Definir Cargo da Ficha</button>
        <button onclick="logoutAdmin()" class="px-3 py-2 bg-rose-50 text-rose-700 border border-rose-200 rounded-lg text-xs font-semibold">Sair</button>
      </div>
    </div>

    <!-- Tabela de Candidaturas -->
    <div class="bg-white border rounded-xl overflow-hidden shadow-xs">
      <div class="p-4 border-b flex justify-between items-center bg-slate-50">
        <span class="text-xs font-bold uppercase tracking-wider text-slate-600">Currículos Recebidos</span>
        <button onclick="exportarCSV()" class="text-xs font-semibold text-slate-700 hover:text-slate-900 bg-white border px-3 py-1.5 rounded-lg">Exportar CSV</button>
      </div>
      <div class="overflow-x-auto">
        <table class="w-full text-left text-xs">
          <thead class="bg-slate-100 text-slate-600 uppercase text-[10px] font-bold">
            <tr>
              <th class="p-3">Data / Hora</th>
              <th class="p-3">Candidato</th>
              <th class="p-3">Cargo Pretendido</th>
              <th class="p-3">Telefone</th>
              <th class="p-3">Cidade/UF</th>
              <th class="p-3 text-right">Ação</th>
            </tr>
          </thead>
          <tbody id="tabelaCurriculos" class="divide-y divide-slate-100"></tbody>
        </table>
      </div>
    </div>
  </main>

  <!-- Modal de Senha -->
  <div id="modalSenha" class="fixed inset-0 bg-slate-900/60 backdrop-blur-xs hidden z-50 flex items-center justify-center p-4">
    <div class="bg-white rounded-2xl max-w-sm w-full p-6 shadow-2xl">
      <h3 class="text-base font-bold text-slate-900 font-display">Acesso do Recrutador</h3>
      <p class="text-xs text-slate-500 mt-1 mb-4">Digite a senha de segurança (Brasa@2026):</p>
      <input type="password" id="inputSenha" placeholder="Senha do recrutador" class="w-full p-2.5 text-sm border rounded-xl mb-3">
      <p id="msgErroSenha" class="text-xs text-rose-600 hidden mb-2 font-medium">Senha incorreta!</p>
      <div class="flex gap-2">
        <button onclick="fecharModalSenha()" class="flex-1 py-2 text-xs bg-slate-100 rounded-xl">Cancelar</button>
        <button onclick="validarSenha()" class="flex-1 py-2 text-xs bg-slate-900 text-white font-bold rounded-xl">Entrar</button>
      </div>
    </div>
  </div>

  <script>
    let cargoAtual = localStorage.getItem('cargo_brasa') || 'Churrasqueiro Profissional';
    const params = new URLSearchParams(window.location.search);
    if (params.get('cargo')) cargoAtual = params.get('cargo');
    document.getElementById('displayCargo').innerText = cargoAtual;

    function abrirModalSenha() {
      document.getElementById('modalSenha').classList.remove('hidden');
      document.getElementById('inputSenha').value = '';
      document.getElementById('msgErroSenha').classList.add('hidden');
    }
    function fecharModalSenha() {
      document.getElementById('modalSenha').classList.add('hidden');
    }
    function validarSenha() {
      const s = document.getElementById('inputSenha').value;
      if (s === 'Brasa@2026') {
        fecharModalSenha();
        sessionStorage.setItem('recruiter_auth', 'true');
        mostrarView('admin');
        renderizarTabela();
      } else {
        document.getElementById('msgErroSenha').classList.remove('hidden');
      }
    }
    function logoutAdmin() {
      sessionStorage.removeItem('recruiter_auth');
      mostrarView('form');
    }
    function mostrarView(view) {
      if (view === 'admin') {
        document.getElementById('viewForm').classList.add('hidden');
        document.getElementById('viewAdmin').classList.remove('hidden');
        document.getElementById('btnVoltarForm').classList.remove('hidden');
        document.getElementById('btnAbrirAdmin').classList.add('hidden');
      } else {
        document.getElementById('viewForm').classList.remove('hidden');
        document.getElementById('viewAdmin').classList.add('hidden');
        document.getElementById('btnVoltarForm').classList.add('hidden');
        document.getElementById('btnAbrirAdmin').classList.remove('hidden');
      }
    }
    function definirNovoCargo() {
      const novo = prompt('Informe o novo cargo da vaga para a ficha:', cargoAtual);
      if (novo && novo.trim()) {
        cargoAtual = novo.trim();
        localStorage.setItem('cargo_brasa', cargoAtual);
        document.getElementById('displayCargo').innerText = cargoAtual;
        alert('Cargo atualizado para: ' + cargoAtual + '\nOs candidatos verão este cargo na ficha.');
      }
    }
    function calcularIdade(dataStr) {
      if (!dataStr) return;
      const b = new Date(dataStr);
      const hoje = new Date();
      let age = hoje.getFullYear() - b.getFullYear();
      if (hoje.getMonth() < b.getMonth() || (hoje.getMonth() === b.getMonth() && hoje.getDate() < b.getDate())) age--;
      if (age >= 14) document.getElementById('campoIdade').value = age;
    }
    async function buscarCep(val) {
      const clean = val.replace(/\D/g, '');
      if (clean.length === 8) {
        try {
          const res = await fetch(`https://viacep.com.br/ws/${clean}/json/`);
          const d = await res.json();
          if (!d.erro) {
            document.getElementById('campoRua').value = d.logradouro || '';
            document.getElementById('campoBairro').value = d.bairro || '';
            document.getElementById('campoCidade').value = d.localidade || '';
            document.getElementById('campoUf').value = d.uf || '';
          }
        } catch(e){}
      }
    }
    function getLista() {
      try {
        return JSON.parse(localStorage.getItem('candidatos_brasa') || '[]');
      } catch(e) { return []; }
    }
    function enviarCandidatura(e) {
      e.preventDefault();
      const agora = new Date();
      const dataStr = agora.toLocaleDateString('pt-BR') + ' às ' + agora.toLocaleTimeString('pt-BR', {hour:'2-digit', minute:'2-digit'});
      const cand = {
        id: Date.now(),
        data: dataStr,
        cargo: cargoAtual,
        nome: document.getElementById('campoNome').value,
        idade: document.getElementById('campoIdade').value,
        nasc: document.getElementById('campoNasc').value,
        cpf: document.getElementById('campoCpf').value,
        rg: document.getElementById('campoRg').value,
        tel: document.getElementById('campoTel').value,
        email: document.getElementById('campoEmail').value,
        endereco: document.getElementById('campoRua').value + ' - ' + document.getElementById('campoCidade').value + '/' + document.getElementById('campoUf').value,
        escolaridade: document.getElementById('campoEscolaridade').value,
        experiencia: document.getElementById('campoExp').value,
        cursos: document.getElementById('campoCursos').value
      };
      const lista = getLista();
      lista.unshift(cand);
      localStorage.setItem('candidatos_brasa', JSON.stringify(lista));
      alert('Candidatura enviada com sucesso para a vaga: ' + cargoAtual + '!');
      document.getElementById('candidateForm').reset();
    }
    function renderizarTabela() {
      const lista = getLista();
      const tbody = document.getElementById('tabelaCurriculos');
      if (lista.length === 0) {
        tbody.innerHTML = '<tr><td colspan="6" class="p-6 text-center text-slate-400">Nenhum currículo cadastrado ainda.</td></tr>';
        return;
      }
      tbody.innerHTML = lista.map(c => `
        <tr class="hover:bg-slate-50">
          <td class="p-3 font-mono">${c.data}</td>
          <td class="p-3 font-bold">${c.nome} (${c.idade}a)</td>
          <td class="p-3"><span class="bg-amber-100 text-amber-900 px-2 py-0.5 rounded text-[11px] font-semibold">${c.cargo}</span></td>
          <td class="p-3">${c.tel}</td>
          <td class="p-3">${c.endereco}</td>
          <td class="p-3 text-right">
            <button onclick='gerarPDF(${JSON.stringify(c)})' class="px-2.5 py-1 bg-slate-900 text-white text-[11px] rounded font-semibold hover:bg-slate-800">PDF</button>
          </td>
        </tr>
      `).join('');
    }
    function gerarPDF(c) {
      const { jsPDF } = window.jspdf;
      const doc = new jsPDF();
      doc.setFillColor(30, 41, 59);
      doc.rect(0, 0, 210, 26, 'F');
      doc.setTextColor(255, 255, 255);
      doc.setFontSize(14);
      doc.text("GRUPO BRASA - FICHA DE REGISTRO DE CANDIDATO", 15, 12);
      doc.setFontSize(9);
      doc.text(`Vaga Pretendida: ${c.cargo.toUpperCase()} | Cadastro: ${c.data}`, 15, 20);

      doc.setTextColor(20, 20, 20);
      doc.setFontSize(11);
      doc.text(`Nome Completo: ${c.nome} | Idade: ${c.idade} anos`, 15, 36);
      doc.text(`Telefone: ${c.tel} | E-mail: ${c.email}`, 15, 43);
      doc.text(`CPF: ${c.cpf || 'Não inf.'} | RG: ${c.rg || 'Não inf.'}`, 15, 50);
      doc.text(`Endereço: ${c.endereco}`, 15, 57);
      doc.text(`Escolaridade: ${c.escolaridade}`, 15, 64);
      doc.text(`Experiência Profissional:`, 15, 74);
      doc.setFontSize(9.5);
      doc.text(doc.splitTextToSize(c.experiencia || 'Sem experiência anterior informada.', 180), 15, 80);
      doc.setFontSize(11);
      doc.text(`Cursos e Qualificações:`, 15, 110);
      doc.setFontSize(9.5);
      doc.text(doc.splitTextToSize(c.cursos || 'Nenhum curso informado.', 180), 15, 116);

      doc.save(`Curriculo_${c.nome.replace(/\\s+/g, '_')}.pdf`);
    }
    function exportarCSV() {
      const lista = getLista();
      let csv = 'Data;Nome;Idade;Cargo;Telefone;Email;Endereco;Escolaridade\n';
      lista.forEach(c => {
        csv += `"${c.data}";"${c.nome}";"${c.idade}";"${c.cargo}";"${c.tel}";"${c.email}";"${c.endereco}";"${c.escolaridade}"\n`;
      });
      const blob = new Blob(["\uFEFF" + csv], { type: 'text/csv;charset=utf-8;' });
      const a = document.createElement('a');
      a.href = URL.createObjectURL(blob);
      a.download = 'candidaturas_brasa.csv';
      a.click();
    }
    function baixarEsteHtml() {
      const htmlContent = '<!DOCTYPE html>\n' + document.documentElement.outerHTML;
      const blob = new Blob([htmlContent], { type: 'text/html;charset=utf-8;' });
      const a = document.createElement('a');
      a.href = URL.createObjectURL(blob);
      a.download = 'ficha_de_emprego_grupo_brasa.html';
      a.click();
    }
  </script>
</body>
</html>
